"use strict";
{
    const name = 'Max';
    let age = 30;
    age = 29;
    console.log(name + age);
    function add(a, b) {
        var result;
        result = a + b;
        return result;
    }
    console.log(add(30, 30));
    if (age >= 20) {
        var isAdult = true;
    }
}
//# sourceMappingURL=app.js.map